[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[[source]]
url = "https://download.pytorch.org/whl/cpu"
verify_ssl = false
name = "torch-cpu"

[packages]
quart = ">=0.19.3"
pandas = ">=2.1.1"
pathlib = ">=1.0.1"
python-dotenv = ">=1.0.0"
scikit-learn = ">=1.3.1"
torch = {version = ">=2.0.1+cpu", index = "torch-cpu"}
tensorflow = ">=2.15.0.post1"
keras = ">=2.15.0"

[dev-packages]
pytest = ">=7.4.2"
pytest-asyncio = ">=0.21.1"

[scripts]
start = "python api.py"

[requires]
python_version = "3.11"
